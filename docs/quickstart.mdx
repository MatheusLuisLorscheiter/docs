---
title: "Quickstart"
description: "Comece a usar a API Disparador em 5 minutos"
---

<Note>
Este guia te ajudará a começar com a API Disparador em poucos minutos. Siga os passos abaixo para configurar sua primeira campanha.
</Note>

## 1. Registre sua empresa

Primeiro, você precisa registrar sua empresa para obter um access token:

<RequestExample>

```bash cURL
curl -X POST https://{url-key}/api/auth/register \
  -H "Content-Type: application/json" \
  -d '{
    "name": "Minha Empresa",
    "email": "contato@minhaempresa.com",
    "password": "senha123"
  }'
```

```javascript JavaScript
const response = await fetch('https://{url-key}/api/auth/register', {
  method: 'POST',
  headers: {
    'Content-Type': 'application/json'
  },
  body: JSON.stringify({
    name: 'Minha Empresa',
    email: 'contato@minhaempresa.com',
    password: 'senha123'
  })
});

const result = await response.json();
console.log('Access Token:', result.accessToken);
```

```python Python
import requests

data = {
    'name': 'Minha Empresa',
    'email': 'contato@minhaempresa.com',
    'password': 'senha123'
}

response = requests.post(
    'https://{url-key}/api/auth/register',
    json=data
)

result = response.json()
print('Access Token:', result['accessToken'])
```

```php PHP
<?php
$data = [
    'name' => 'Minha Empresa',
    'email' => 'contato@minhaempresa.com',
    'password' => 'senha123'
];

$curl = curl_init();
curl_setopt_array($curl, [
    CURLOPT_URL => 'https://{url-key}/api/auth/register',
    CURLOPT_RETURNTRANSFER => true,
    CURLOPT_POST => true,
    CURLOPT_HTTPHEADER => ['Content-Type: application/json'],
    CURLOPT_POSTFIELDS => json_encode($data)
]);

$response = curl_exec($curl);
curl_close($curl);

$result = json_decode($response, true);
echo 'Access Token: ' . $result['accessToken'];
?>
```

</RequestExample>

<ResponseExample>

```json Sucesso (200 OK)
{
  "accessToken": "a1b2c3d4-e5f6-7890-abcd-ef1234567890",
  "tokenType": "Access",
  "companyId": 1,
  "companyName": "Minha Empresa",
  "companyEmail": "contato@minhaempresa.com",
  "expiresAt": null,
  "message": null
}
```

</ResponseExample>

## 2. Configure sua Evolution API

<Warning>
Certifique-se de que sua Evolution API está configurada e funcionando antes de prosseguir.
</Warning>

Você precisará fornecer nas requisições:

<ParamField body="evolutionUrl" type="string" required>
  URL da sua Evolution API (ex: https://sua-evolution-api.com)
</ParamField>

<ParamField body="evolutionApiKey" type="string" required>
  Sua chave de API da Evolution
</ParamField>

<ParamField body="evolutionInstance" type="string" required>
  Nome da instância do WhatsApp conectada
</ParamField>

## 3. Crie sua primeira campanha

Agora vamos criar uma campanha simples de texto:

<RequestExample>

```bash cURL
curl -X POST https://{url-key}/api/campaigns \
  -H "Content-Type: application/json" \
  -H "X-Access-Token: {access_token}" \
  -d '{
    "name": "Campanha de Boas-vindas",
    "evolutionUrl": "https://sua-evolution-api.com",
    "evolutionApiKey": "sua-api-key",
    "evolutionInstance": "minha-instancia",
    "message": "Olá {{PrimeiroNome}}! Bem-vindo à nossa empresa!",
    "contacts": [
      {
        "phoneNumber": "5511999999999",
        "name": "João Silva"
      },
      {
        "phoneNumber": "5511888888888",
        "name": "Maria Santos"
      }
    ]
  }'
```

```javascript JavaScript
const campaignData = {
  name: 'Campanha de Boas-vindas',
  evolutionUrl: 'https://sua-evolution-api.com',
  evolutionApiKey: 'sua-api-key',
  evolutionInstance: 'minha-instancia',
  message: 'Olá {{PrimeiroNome}}! Bem-vindo à nossa empresa!',
  contacts: [
    {
      phoneNumber: '5511999999999',
      name: 'João Silva'
    },
    {
      phoneNumber: '5511888888888',
      name: 'Maria Santos'
    }
  ]
};

const response = await fetch('https://{url-key}/api/campaigns', {
  method: 'POST',
  headers: {
    'Content-Type': 'application/json',
    'X-Access-Token': '{access_token}'
  },
  body: JSON.stringify(campaignData)
});

const result = await response.json();
console.log('Campanha criada:', result);
```

```python Python
import requests

campaign_data = {
    'name': 'Campanha de Boas-vindas',
    'evolutionUrl': 'https://sua-evolution-api.com',
    'evolutionApiKey': 'sua-api-key',
    'evolutionInstance': 'minha-instancia',
    'message': 'Olá {{PrimeiroNome}}! Bem-vindo à nossa empresa!',
    'contacts': [
        {
            'phoneNumber': '5511999999999',
            'name': 'João Silva'
        },
        {
            'phoneNumber': '5511888888888',
            'name': 'Maria Santos'
        }
    ]
}

headers = {
    'Content-Type': 'application/json',
    'X-Access-Token': '{access_token}'
}

response = requests.post(
    'https://{url-key}/api/campaigns',
    json=campaign_data,
    headers=headers
)

result = response.json()
print('Campanha criada:', result)
```

```php PHP
<?php
$campaign_data = [
    'name' => 'Campanha de Boas-vindas',
    'evolutionUrl' => 'https://sua-evolution-api.com',
    'evolutionApiKey' => 'sua-api-key',
    'evolutionInstance' => 'minha-instancia',
    'message' => 'Olá {{PrimeiroNome}}! Bem-vindo à nossa empresa!',
    'contacts' => [
        [
            'phoneNumber' => '5511999999999',
            'name' => 'João Silva'
        ],
        [
            'phoneNumber' => '5511888888888',
            'name' => 'Maria Santos'
        ]
    ]
];

$headers = [
    'Content-Type: application/json',
    'X-Access-Token: {access_token}'
];

$curl = curl_init();
curl_setopt_array($curl, [
    CURLOPT_URL => 'https://{url-key}/api/campaigns',
    CURLOPT_RETURNTRANSFER => true,
    CURLOPT_POST => true,
    CURLOPT_HTTPHEADER => $headers,
    CURLOPT_POSTFIELDS => json_encode($campaign_data)
]);

$response = curl_exec($curl);
curl_close($curl);

$result = json_decode($response, true);
print_r($result);
?>
```

</RequestExample>

<ResponseExample>

```json Sucesso (201 Created)
{
  "id": 1,
  "name": "Campanha de Boas-vindas",
  "status": "ACTIVE",
  "totalContacts": 2,
  "sentMessages": 0,
  "failedMessages": 0,
  "message": null
}
```

</ResponseExample>

## 4. Acompanhe o progresso

Verifique o status da sua campanha:

<RequestExample>

```bash cURL
curl -X GET https://{url-key}/api/campaigns/{campaign_id} \
  -H "X-Access-Token: {access_token}"
```

```javascript JavaScript
const campaignId = {campaign_id};
const response = await fetch(`https://{url-key}/api/campaigns/${campaignId}`, {
  headers: {
    'X-Access-Token': '{access_token}'
  }
});

const result = await response.json();
console.log('Status da campanha:', result);
```

```python Python
import requests

campaign_id = {campaign_id}
headers = {
    'X-Access-Token': '{access_token}'
}

response = requests.get(
    f'https://{url-key}/api/campaigns/{campaign_id}',
    headers=headers
)

result = response.json()
print('Status da campanha:', result)
```

```php PHP
<?php
$campaign_id = {campaign_id};
$headers = [
    'X-Access-Token: {access_token}'
];

$curl = curl_init();
curl_setopt_array($curl, [
    CURLOPT_URL => "https://{url-key}/api/campaigns/{$campaign_id}",
    CURLOPT_RETURNTRANSFER => true,
    CURLOPT_HTTPHEADER => $headers
]);

$response = curl_exec($curl);
curl_close($curl);

$result = json_decode($response, true);
print_r($result);
?>
```

</RequestExample>

<ResponseExample>

```json Sucesso (200 OK)
{
  "id": 1,
  "name": "Campanha de Boas-vindas",
  "status": "ACTIVE",
  "totalContacts": 2,
  "sentMessages": 2,
  "failedMessages": 0,
  "createdAt": "2024-01-15T10:30:00",
  "message": null
}
```

</ResponseExample>

## 5. Exemplo com mídia

Crie uma campanha com imagem:

<RequestExample>

```bash cURL
curl -X POST https://{url-key}/api/campaigns \
  -H "Content-Type: application/json" \
  -H "X-Access-Token: {access_token}" \
  -d '{
    "name": "Campanha com Imagem",
    "evolutionUrl": "https://sua-evolution-api.com",
    "evolutionApiKey": "sua-api-key",
    "evolutionInstance": "minha-instancia",
    "message": "Confira nossa nova promoção!",
    "mediaUrl": "https://exemplo.com/imagem.jpg",
    "mediaType": "image",
    "caption": "Oferta especial para você!",
    "contacts": [
      {
        "phoneNumber": "5511999999999",
        "name": "João Silva"
      }
    ]
  }'
```

```javascript JavaScript
const mediaCampaign = {
  name: 'Campanha com Imagem',
  evolutionUrl: 'https://sua-evolution-api.com',
  evolutionApiKey: 'sua-api-key',
  evolutionInstance: 'minha-instancia',
  message: 'Confira nossa nova promoção!',
  mediaUrl: 'https://exemplo.com/imagem.jpg',
  mediaType: 'image',
  caption: 'Oferta especial para você!',
  contacts: [
    {
      phoneNumber: '5511999999999',
      name: 'João Silva'
    }
  ]
};

const response = await fetch('https://{url-key}/api/campaigns', {
  method: 'POST',
  headers: {
    'Content-Type': 'application/json',
    'X-Access-Token': '{access_token}'
  },
  body: JSON.stringify(mediaCampaign)
});

const result = await response.json();
console.log('Campanha com mídia criada:', result);
```

```python Python
import requests

media_campaign = {
    'name': 'Campanha com Imagem',
    'evolutionUrl': 'https://sua-evolution-api.com',
    'evolutionApiKey': 'sua-api-key',
    'evolutionInstance': 'minha-instancia',
    'message': 'Confira nossa nova promoção!',
    'mediaUrl': 'https://exemplo.com/imagem.jpg',
    'mediaType': 'image',
    'caption': 'Oferta especial para você!',
    'contacts': [
        {
            'phoneNumber': '5511999999999',
            'name': 'João Silva'
        }
    ]
}

headers = {
    'Content-Type': 'application/json',
    'X-Access-Token': '{access_token}'
}

response = requests.post(
    'https://{url-key}/api/campaigns',
    json=media_campaign,
    headers=headers
)

result = response.json()
print('Campanha com mídia criada:', result)
```

```php PHP
<?php
$media_campaign = [
    'name' => 'Campanha com Imagem',
    'evolutionUrl' => 'https://sua-evolution-api.com',
    'evolutionApiKey' => 'sua-api-key',
    'evolutionInstance' => 'minha-instancia',
    'message' => 'Confira nossa nova promoção!',
    'mediaUrl' => 'https://exemplo.com/imagem.jpg',
    'mediaType' => 'image',
    'caption' => 'Oferta especial para você!',
    'contacts' => [
        [
            'phoneNumber' => '5511999999999',
            'name' => 'João Silva'
        ]
    ]
];

$headers = [
    'Content-Type: application/json',
    'X-Access-Token: {access_token}'
];

$curl = curl_init();
curl_setopt_array($curl, [
    CURLOPT_URL => 'https://{url-key}/api/campaigns',
    CURLOPT_RETURNTRANSFER => true,
    CURLOPT_POST => true,
    CURLOPT_HTTPHEADER => $headers,
    CURLOPT_POSTFIELDS => json_encode($media_campaign)
]);

$response = curl_exec($curl);
curl_close($curl);

$result = json_decode($response, true);
print_r($result);
?>
```

</RequestExample>

## 6. Agendamento

Agende uma campanha para execução futura:

<RequestExample>

```bash cURL
curl -X POST https://{url-key}/api/campaigns \
  -H "Content-Type: application/json" \
  -H "X-Access-Token: {access_token}" \
  -d '{
    "name": "Campanha Agendada",
    "evolutionUrl": "https://sua-evolution-api.com",
    "evolutionApiKey": "sua-api-key",
    "evolutionInstance": "minha-instancia",
    "message": "Lembrete: Reunião amanhã às 10h!",
    "scheduledFor": "2024-01-16T09:00:00",
    "contacts": [
      {
        "phoneNumber": "5511999999999",
        "name": "João Silva"
      }
    ]
  }'
```

```javascript JavaScript
const scheduledCampaign = {
  name: 'Campanha Agendada',
  evolutionUrl: 'https://sua-evolution-api.com',
  evolutionApiKey: 'sua-api-key',
  evolutionInstance: 'minha-instancia',
  message: 'Lembrete: Reunião amanhã às 10h!',
  scheduledFor: '2024-01-16T09:00:00',
  contacts: [
    {
      phoneNumber: '5511999999999',
      name: 'João Silva'
    }
  ]
};

const response = await fetch('https://{url-key}/api/campaigns', {
  method: 'POST',
  headers: {
    'Content-Type': 'application/json',
    'X-Access-Token': '{access_token}'
  },
  body: JSON.stringify(scheduledCampaign)
});

const result = await response.json();
console.log('Campanha agendada:', result);
```

```python Python
import requests

scheduled_campaign = {
    'name': 'Campanha Agendada',
    'evolutionUrl': 'https://sua-evolution-api.com',
    'evolutionApiKey': 'sua-api-key',
    'evolutionInstance': 'minha-instancia',
    'message': 'Lembrete: Reunião amanhã às 10h!',
    'scheduledFor': '2024-01-16T09:00:00',
    'contacts': [
        {
            'phoneNumber': '5511999999999',
            'name': 'João Silva'
        }
    ]
}

headers = {
    'Content-Type': 'application/json',
    'X-Access-Token': '{access_token}'
}

response = requests.post(
    'https://{url-key}/api/campaigns',
    json=scheduled_campaign,
    headers=headers
)

result = response.json()
print('Campanha agendada:', result)
```

```php PHP
<?php
$scheduled_campaign = [
    'name' => 'Campanha Agendada',
    'evolutionUrl' => 'https://sua-evolution-api.com',
    'evolutionApiKey' => 'sua-api-key',
    'evolutionInstance' => 'minha-instancia',
    'message' => 'Lembrete: Reunião amanhã às 10h!',
    'scheduledFor' => '2024-01-16T09:00:00',
    'contacts' => [
        [
            'phoneNumber' => '5511999999999',
            'name' => 'João Silva'
        ]
    ]
];

$headers = [
    'Content-Type: application/json',
    'X-Access-Token: {access_token}'
];

$curl = curl_init();
curl_setopt_array($curl, [
    CURLOPT_URL => 'https://{url-key}/api/campaigns',
    CURLOPT_RETURNTRANSFER => true,
    CURLOPT_POST => true,
    CURLOPT_HTTPHEADER => $headers,
    CURLOPT_POSTFIELDS => json_encode($scheduled_campaign)
]);

$response = curl_exec($curl);
curl_close($curl);

$result = json_decode($response, true);
print_r($result);
?>
```

</RequestExample>

## 7. Gerenciamento de campanhas

<CardGroup cols={3}>
  <Card title="Pausar Campanha" icon="pause" color="#eab308">
    Interrompe temporariamente o envio
  </Card>
  <Card title="Retomar Campanha" icon="play" color="#16a34a">
    Continua uma campanha pausada
  </Card>
  <Card title="Cancelar Campanha" icon="stop" color="#dc2626">
    Cancela permanentemente a campanha
  </Card>
</CardGroup>

### Pausar campanha

<RequestExample>

```bash cURL
curl -X PUT https://{url-key}/api/campaigns/{campaign_id}/pause \
  -H "X-Access-Token: {access_token}"
```

```javascript JavaScript
const campaignId = {campaign_id};
const response = await fetch(`https://{url-key}/api/campaigns/${campaignId}/pause`, {
  method: 'PUT',
  headers: {
    'X-Access-Token': '{access_token}'
  }
});

const result = await response.json();
console.log('Campanha pausada:', result);
```

</RequestExample>

### Retomar campanha

<RequestExample>

```bash cURL
curl -X PUT https://{url-key}/api/campaigns/{campaign_id}/resume \
  -H "X-Access-Token: {access_token}"
```

```javascript JavaScript
const campaignId = {campaign_id};
const response = await fetch(`https://{url-key}/api/campaigns/${campaignId}/resume`, {
  method: 'PUT',
  headers: {
    'X-Access-Token': '{access_token}'
  }
});

const result = await response.json();
console.log('Campanha retomada:', result);
```

</RequestExample>

### Cancelar campanha

<RequestExample>

```bash cURL
curl -X PUT https://{url-key}/api/campaigns/{campaign_id}/cancel \
  -H "X-Access-Token: {access_token}"
```

```javascript JavaScript
const campaignId = {campaign_id};
const response = await fetch(`https://{url-key}/api/campaigns/${campaignId}/cancel`, {
  method: 'PUT',
  headers: {
    'X-Access-Token': '{access_token}'
  }
});

const result = await response.json();
console.log('Campanha cancelada:', result);
```

</RequestExample>

## 8. Listar campanhas

Veja todas as suas campanhas:

<RequestExample>

```bash cURL
curl -X GET https://{url-key}/api/campaigns \
  -H "X-Access-Token: {access_token}"
```

```javascript JavaScript
const response = await fetch('https://{url-key}/api/campaigns', {
  headers: {
    'X-Access-Token': '{access_token}'
  }
});

const campaigns = await response.json();
console.log('Todas as campanhas:', campaigns);
```

```python Python
import requests

headers = {
    'X-Access-Token': '{access_token}'
}

response = requests.get(
    'https://{url-key}/api/campaigns',
    headers=headers
)

campaigns = response.json()
print('Todas as campanhas:', campaigns)
```

```php PHP
<?php
$headers = [
    'X-Access-Token: {access_token}'
];

$curl = curl_init();
curl_setopt_array($curl, [
    CURLOPT_URL => 'https://{url-key}/api/campaigns',
    CURLOPT_RETURNTRANSFER => true,
    CURLOPT_HTTPHEADER => $headers
]);

$response = curl_exec($curl);
curl_close($curl);

$campaigns = json_decode($response, true);
print_r($campaigns);
?>
```

</RequestExample>

## Próximos Passos

Agora que você conhece o básico, explore mais funcionalidades:

<CardGroup cols={2}>
  <Card title="Tipos de Mensagem" icon="message" href="/campaigns/message-types">
    Conheça todos os tipos de mensagem suportados
  </Card>
  <Card title="Variáveis Personalizadas" icon="brackets-curly" href="/integration/variables">
    Personalize suas mensagens com variáveis
  </Card>
  <Card title="Agendamento Avançado" icon="clock" href="/integration/scheduling">
    Configure campanhas para execução futura
  </Card>
  <Card title="API Reference" icon="code" href="/api-reference/auth/register">
    Documentação completa da API
  </Card>
</CardGroup>

## Precisa de ajuda?

<CardGroup cols={3}>
  <Card title="Suporte por Email" icon="envelope">
    suporte@disparador.com
  </Card>
  <Card title="GitHub Issues" icon="github" href="https://github.com/matheusluislorscheiter/api-disparador/issues">
    Reporte bugs e solicite features
  </Card>
  <Card title="Documentação" icon="book" href="/api-reference/auth/register">
    Consulte a documentação completa
  </Card>
</CardGroup>