---
title: "Informações da Aplicação"
api: "GET /actuator/info"
description: "Obtém informações sobre a aplicação, versão e ambiente"
---

<Note>
O endpoint de informações fornece dados sobre a versão da aplicação, Java runtime e configurações do sistema.
</Note>

## Visão Geral

Este endpoint do Spring Boot Actuator fornece informações úteis sobre:

- **Versão** - Versão atual da aplicação
- **Build** - Informações de compilação
- **Java** - Versão e detalhes do runtime
- **Sistema** - Sistema operacional e arquitetura

## Endpoint Público

Este endpoint não requer autenticação e está disponível publicamente.

<RequestExample>

```bash cURL
curl -X GET https://api.disparador.com/actuator/info
```

```javascript JavaScript
const response = await fetch('https://api.disparador.com/actuator/info');
const info = await response.json();

console.log('🏷️ Aplicação:', info.app?.name);
console.log('📦 Versão:', info.app?.version);
console.log('☕ Java:', info.java?.version);

// Verificar se está atualizado
if (info.app?.version) {
  const [major, minor, patch] = info.app.version.split('.');
  console.log(`Versão ${major}.${minor}.${patch}`);
}
```

```python Python
import requests

response = requests.get('https://api.disparador.com/actuator/info')
info = response.json()

print("=== Informações da API ===")
if 'app' in info:
    print(f"Nome: {info['app'].get('name', 'N/A')}")
    print(f"Versão: {info['app'].get('version', 'N/A')}")
    print(f"Descrição: {info['app'].get('description', 'N/A')}")

if 'java' in info:
    print(f"\n=== Java Runtime ===")
    print(f"Versão: {info['java']['version']}")
    print(f"Vendor: {info['java']['vendor']}")
```

```php PHP
<?php
$url = 'https://api.disparador.com/actuator/info';
$result = file_get_contents($url);
$info = json_decode($result, true);

echo "=== Informações da API ===\n";
if (isset($info['app'])) {
    echo "Nome: " . ($info['app']['name'] ?? 'N/A') . "\n";
    echo "Versão: " . ($info['app']['version'] ?? 'N/A') . "\n";
}

if (isset($info['java'])) {
    echo "\n=== Java Runtime ===\n";
    echo "Versão: " . $info['java']['version'] . "\n";
    echo "Vendor: " . $info['java']['vendor'] . "\n";
}
?>
```

</RequestExample>

## Response

<ResponseField name="app" type="object">
  Informações da aplicação
  
  <Expandable title="Campos da aplicação">
    <ResponseField name="name" type="string">
      Nome da aplicação
    </ResponseField>
    
    <ResponseField name="description" type="string">
      Descrição da aplicação
    </ResponseField>
    
    <ResponseField name="version" type="string">
      Versão atual da aplicação
    </ResponseField>
    
    <ResponseField name="timezone" type="string">
      Timezone configurado
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="build" type="object">
  Informações de build
  
  <Expandable title="Detalhes do build">
    <ResponseField name="artifact" type="string">
      Nome do artefato
    </ResponseField>
    
    <ResponseField name="time" type="string">
      Data/hora do build
    </ResponseField>
    
    <ResponseField name="version" type="string">
      Versão do build
    </ResponseField>
    
    <ResponseField name="group" type="string">
      Grupo do artefato
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="java" type="object">
  Informações do Java
  
  <Expandable title="Detalhes do Java">
    <ResponseField name="version" type="string">
      Versão do Java
    </ResponseField>
    
    <ResponseField name="vendor" type="string">
      Fornecedor do Java
    </ResponseField>
    
    <ResponseField name="runtime" type="object">
      Detalhes do runtime
    </ResponseField>
    
    <ResponseField name="jvm" type="object">
      Detalhes da JVM
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="os" type="object">
  Informações do sistema operacional
  
  <Expandable title="Detalhes do SO">
    <ResponseField name="name" type="string">
      Nome do sistema operacional
    </ResponseField>
    
    <ResponseField name="version" type="string">
      Versão do SO
    </ResponseField>
    
    <ResponseField name="arch" type="string">
      Arquitetura (amd64, arm64, etc)
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseExample>

```json 200 - Informações Completas
{
  "app": {
    "name": "disparador-api",
    "description": "Sistema de disparo de mensagens via WhatsApp",
    "version": "2.1.0",
    "timezone": "America/Sao_Paulo"
  },
  "build": {
    "artifact": "disparador-api",
    "name": "disparador-api", 
    "time": "2024-01-20T10:30:00.000Z",
    "version": "2.1.0",
    "group": "com.disparador"
  },
  "java": {
    "version": "17.0.10",
    "vendor": "Eclipse Adoptium",
    "runtime": {
      "name": "OpenJDK Runtime Environment",
      "version": "17.0.10+7"
    },
    "jvm": {
      "name": "OpenJDK 64-Bit Server VM",
      "vendor": "Eclipse Adoptium",
      "version": "17.0.10+7"
    }
  },
  "os": {
    "name": "Linux",
    "version": "6.8.0",
    "arch": "amd64"
  },
  "process": {
    "pid": 1
  }
}
```

```json 200 - Informações Mínimas
{
  "app": {
    "name": "disparador-api",
    "version": "2.1.0"
  },
  "java": {
    "version": "17.0.10"
  }
}
```

</ResponseExample>

## Uso Comum

### Verificar Versão

```javascript
async function checkVersion(requiredVersion) {
  const response = await fetch('https://api.disparador.com/actuator/info');
  const info = await response.json();
  
  const currentVersion = info.app?.version || '0.0.0';
  const [curMajor, curMinor, curPatch] = currentVersion.split('.').map(Number);
  const [reqMajor, reqMinor, reqPatch] = requiredVersion.split('.').map(Number);
  
  if (curMajor < reqMajor) return false;
  if (curMajor > reqMajor) return true;
  if (curMinor < reqMinor) return false;
  if (curMinor > reqMinor) return true;
  return curPatch >= reqPatch;
}

// Verificar se API está na versão 2.0.0 ou superior
const isCompatible = await checkVersion('2.0.0');
if (!isCompatible) {
  console.warn('API em versão incompatível!');
}
```

### Dashboard de Informações

```javascript
async function displaySystemInfo() {
  const response = await fetch('https://api.disparador.com/actuator/info');
  const info = await response.json();
  
  const dashboard = {
    'Aplicação': info.app?.name || 'Desconhecida',
    'Versão': info.app?.version || 'N/A',
    'Java': info.java?.version || 'N/A',
    'SO': `${info.os?.name} ${info.os?.version}` || 'N/A',
    'Arquitetura': info.os?.arch || 'N/A',
    'Timezone': info.app?.timezone || 'UTC',
    'Build Time': info.build?.time ? new Date(info.build.time).toLocaleString() : 'N/A'
  };
  
  console.table(dashboard);
  return dashboard;
}
```

## Integração com CI/CD

```yaml
# GitHub Actions - Verificar versão após deploy
- name: Verify Deployment
  run: |
    VERSION=$(curl -s https://api.disparador.com/actuator/info | jq -r '.app.version')
    echo "Deployed version: $VERSION"
    
    if [ "$VERSION" != "${{ github.ref_name }}" ]; then
      echo "Version mismatch! Expected ${{ github.ref_name }}, got $VERSION"
      exit 1
    fi
```

## Informações Adicionais

<Note>
As informações disponíveis dependem da configuração da aplicação. Nem todos os campos podem estar presentes em todas as instalações.
</Note>

<CardGroup cols={2}>
  <Card title="Versão Semântica" icon="code-branch" color="#3b82f6">
    Formato: MAJOR.MINOR.PATCH (ex: 2.1.0)
  </Card>
  <Card title="Java 17+" icon="coffee" color="#ea580c">
    Requer Java 17 ou superior
  </Card>
  <Card title="Timezone" icon="globe" color="#8b5cf6">
    Padrão: America/Sao_Paulo
  </Card>
  <Card title="Build Info" icon="hammer" color="#16a34a">
    Gerado automaticamente no build
  </Card>
</CardGroup>