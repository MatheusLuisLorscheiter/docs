---
title: 'Quickstart'
description: 'Comece a usar a API Disparador em 5 minutos'
---

# Quickstart

Este guia te ajudará a começar com a API Disparador em poucos minutos.

## 1. Registre sua empresa

Primeiro, você precisa registrar sua empresa para obter um access token:

```bash
curl -X POST https://api.disparador.com/api/auth/register \
  -H "Content-Type: application/json" \
  -d '{
    "name": "Minha Empresa",
    "email": "contato@minhaempresa.com",
    "password": "senha123"
  }'
```

**Resposta:**
```json
{
  "accessToken": "eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9...",
  "companyId": 1,
  "message": null
}
```

## 2. Configure sua Evolution API

Certifique-se de que sua Evolution API está configurada e funcionando. Você precisará fornecer na requisição:

- **URL da Evolution API**: `https://sua-evolution-api.com`
- **API Key**: Sua chave de API da Evolution
- **Instância**: Nome da instância do WhatsApp conectada

## 3. Crie sua primeira campanha

Agora vamos criar uma campanha simples de texto:

```bash
curl -X POST https://api.disparador.com/api/campaigns \
  -H "Content-Type: application/json" \
  -H "X-Access-Token: eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9..." \
  -d '{
    "name": "Campanha de Boas-vindas",
    "evolutionUrl": "https://sua-evolution-api.com",
    "evolutionApiKey": "sua-api-key",
    "evolutionInstance": "minha-instancia",
    "message": "Olá {{name}}! Bem-vindo à nossa empresa!",
    "contacts": [
      {
        "phoneNumber": "5511999999999",
        "name": "João Silva"
      },
      {
        "phoneNumber": "5511888888888",
        "name": "Maria Santos"
      }
    ]
  }'
```

**Resposta:**
```json
{
  "id": 1,
  "name": "Campanha de Boas-vindas",
  "status": "ACTIVE",
  "totalContacts": 2,
  "sentMessages": 0,
  "failedMessages": 0,
  "message": null
}
```

## 4. Acompanhe o progresso

Verifique o status da sua campanha:

```bash
curl -X GET https://api.disparador.com/api/campaigns/1 \
  -H "X-Access-Token: eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9..."
```

**Resposta:**
```json
{
  "id": 1,
  "name": "Campanha de Boas-vindas",
  "status": "ACTIVE",
  "totalContacts": 2,
  "sentMessages": 2,
  "failedMessages": 0,
  "createdAt": "2024-01-15T10:30:00",
  "message": null
}
```

## 5. Exemplo com mídia

Crie uma campanha com imagem:

```bash
curl -X POST https://api.disparador.com/api/campaigns \
  -H "Content-Type: application/json" \
  -H "X-Access-Token: eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9..." \
  -d '{
    "name": "Campanha com Imagem",
    "evolutionUrl": "https://sua-evolution-api.com",
    "evolutionApiKey": "sua-api-key",
    "evolutionInstance": "minha-instancia",
    "message": "Confira nossa nova promoção!",
    "mediaUrl": "https://exemplo.com/imagem.jpg",
    "mediaType": "image",
    "caption": "Oferta especial para você!",
    "contacts": [
      {
        "phoneNumber": "5511999999999",
        "name": "João Silva"
      }
    ]
  }'
```

## 6. Agendamento

Agende uma campanha para execução futura:

```bash
curl -X POST https://api.disparador.com/api/campaigns \
  -H "Content-Type: application/json" \
  -H "X-Access-Token: eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9..." \
  -d '{
    "name": "Campanha Agendada",
    "evolutionUrl": "https://sua-evolution-api.com",
    "evolutionApiKey": "sua-api-key",
    "evolutionInstance": "minha-instancia",
    "message": "Lembrete: Reunião amanhã às 10h!",
    "scheduledFor": "2024-01-16T09:00:00",
    "contacts": [
      {
        "phoneNumber": "5511999999999",
        "name": "João Silva"
      }
    ]
  }'
```

## 7. Gerenciamento de campanhas

### Pausar campanha
```bash
curl -X PUT https://api.disparador.com/api/campaigns/1/pause \
  -H "X-Access-Token: eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9..."
```

### Retomar campanha
```bash
curl -X PUT https://api.disparador.com/api/campaigns/1/resume \
  -H "X-Access-Token: eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9..."
```

### Cancelar campanha
```bash
curl -X PUT https://api.disparador.com/api/campaigns/1/cancel \
  -H "X-Access-Token: eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9..."
```

## 8. Listar campanhas

Veja todas as suas campanhas:

```bash
curl -X GET https://api.disparador.com/api/campaigns \
  -H "X-Access-Token: eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9..."
```

## 🎉 Próximos passos

Agora que você conhece o básico, explore:

- [Tipos de Mensagem](/campaigns/message-types) - Todos os tipos suportados
- [Variáveis Personalizadas](/integration/variables) - Personalize suas mensagens
- [Agendamento](/integration/scheduling) - Configure campanhas futuras
- [API Reference](/api-reference/auth/register) - Documentação completa

## 🆘 Precisa de ajuda?

- **Email**: suporte@disparador.com
- **GitHub**: [Issues](https://github.com/matheusluislorscheiter/api-disparador/issues)
- **Documentação**: [API Reference](/api-reference/auth/register)
